package edu.forty.bits.datastructures.array;

import java.util.Hashtable;
/**
 * Design and implement a hashtable which uses chaining (linked lists) to handle collisions.
 *
 * @param <K> key type
 * @param <V> value type
 */
public class HashtableDesign<K, V> {
    Hashtable<K, V> hashtable = new Hashtable<>();
}