package edu.forty.bits.designing.messaging;

/*
 * Design a messaging framework such as there are thee components: source, destination and queuing service.
 * Source produce the messages and queuing service notifies the destination to consume the relevant messages.
 * The relevance is derived by the queue id of the queue that source produces to and destination consumes from.
 */